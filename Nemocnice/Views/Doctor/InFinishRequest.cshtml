@model IEnumerable<Nemocnice.Models.CheckupTicketToMeFinish>

@{
    ViewData["Title"] = "Příchozí vyřízené žádosti";
}

<h1>Příchozí vyřízené žádosti</h1>

<div class="flex-row row" style="display:flex;margin-bottom:5px">
    <div>
        <form asp-action="InFinishRequest" method="get">
            <div class="form-actions no-color">
                <div class="input-group">
                    <input class="hledat-text" type="text" placeholder="Hledat" name="SearchString" id="search" autocomplete="off" value="@ViewData["Search"]" />
                    <div class="input-group-append">
                        <button type="submit" name="SortOrder" value="@ViewData["SortOrder"]" class="btn btn-primary"><i class="fa fa-search" style="font-size:20px"></i></button>
                    </div>
                </div>
            </div>
        </form>
    </div>
    <div class="col float-right">
        <div class="row">
            <div>
                <button style="margin-left:20px" onclick="redirectToAction('@Url.Action("InFinishRequest", "Doctor", new { })')" class="btn btn-secondary">Zrušit filtry</button>
            </div>
        </div>
    </div>
</div>


<div style="overflow-x: auto">
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.ActionLink("Příjmení", "InFinishRequest", new { sortOrder = "bySurname", searchString = ViewData["Search"] })
                </th>
                <th>
                    @Html.ActionLink("Jméno", "InFinishRequest", new { sortOrder = "byName", searchString = ViewData["Search"] })
                </th>
                <th>
                    @Html.ActionLink("Rodné číslo", "InFinishRequest", new { sortOrder = "byNumber", searchString = ViewData["Search"] })
                </th>
                <th>
                    @Html.ActionLink("Lékař", "InFinishRequest", new { sortOrder = "byDoctor", searchString = ViewData["Search"] })
                </th>
                <th>
                    @Html.ActionLink("Datum vytvoření", "InFinishRequest", new { sortOrder = "byCreateDate", searchString = ViewData["Search"] })
                </th>
                <th>
                    @Html.ActionLink("Datum ošetření", "InFinishRequest", new { sortOrder = "byOsetreniDate", searchString = ViewData["Search"] })
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr onclick="redirectToAction('@Url.Action("CheckupInFin", "Doctor", new { patientNum = item.SocialSecurityNumber, date = item.CreationDate })')">
                    <td>
                        @Html.DisplayFor(modelItem => item.PatientSurname)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.PatientName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.SocialSecurityNumber)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.FromDoctor)
                    </td>
                    <td>
                        @item.GetCreationDateAsString()
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.FinishDate)
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
